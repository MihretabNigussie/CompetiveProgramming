class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
        sum = 0
        for i, j  in enumerate(nums):
            for k,l in enumerate(nums):
                if i<k and j==l:
                    print(i,k)
                    sum += 1

        return sum
