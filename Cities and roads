n = int(input())
grid = []

for _ in range(n):
    row = list(map(int, input().split()))
    grid.append(row)
count = 0

for i in range(n):
    for j in range(n):
        if grid[i][j]:
            count += 1
print(count//2)
