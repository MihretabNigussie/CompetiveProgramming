class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        lst = []
        for index,value in enumerate(sorted(nums)):
            if value == target:
                lst.append(index)
        return lst
